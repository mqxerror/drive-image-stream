// Re-export types from API service for backwards compatibility
export type {
  Project,
  Template,
  Settings,
  Stats,
  QueueItem,
  HistoryItem,
  ProjectImage,
  ProjectImagesResponse,
} from '@/services/api';
